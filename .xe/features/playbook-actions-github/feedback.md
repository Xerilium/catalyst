# Feedback: playbook-actions-github

- **Git actions**: Missing `git-branch-create`, `git-commit`, `git-push` actions. Kitchen-sink section 8.5 uses a 70-line bash script as workaround. Natural fit alongside existing GitHub actions.
- **Rename to playbook-actions-git**: Feature covers both git and GitHub operations. Renaming to `playbook-actions-git` would be more inclusive and accurate.
