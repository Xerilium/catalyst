# Plan: {plan-id}

> [INSTRUCTIONS]
> Tracks the work required for one or more features. Keep updated so work can resume if context window resets. Deleted when complete.

## Overview

> [INSTRUCTIONS]
> Describe what this work accomplishes. Include enough context that a new session can pick up without re-reading the full conversation history.

## Pre-implementation

> [INSTRUCTIONS]
> Tasks that must complete before feature implementation begins. Examples: database migrations, configuration changes, dependency upgrades, infrastructure setup, or data backfills. Delete this section if there are no pre-implementation tasks.

- [ ] Task

## Features

> [INSTRUCTIONS]
> Feature implementation tasks grouped by feature. Use `### {feature-id}` sub-headings to keep features separate and in dependency order. Mark completed tasks with `[x]`. If sub-steps can be figured out from the spec and codebase, don't list them. For multi-plan efforts, add a task linking to follow-on plan files.

### {feature-id}

- [ ] Task

## Post-implementation

> [INSTRUCTIONS]
> Tasks that must complete after feature implementation. Add project-specific tasks (data migrations, cleanup, monitoring, documentation) above the standard closure tasks.

- [ ] Present work for review
- [ ] Route external issues discovered during implementation
- [ ] Clean up temporary files and this plan doc
- [ ] Close out — commit, PR, or defer

## Notes

> [INSTRUCTIONS]
> Design decisions made during implementation, blockers encountered, and anything needed to resume work. Append new notes as work progresses — do not overwrite previous entries.
